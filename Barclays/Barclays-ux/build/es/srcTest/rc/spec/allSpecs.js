define(['nmodule/js/rc/jasmine/promiseUtils', 'nmodule/BarclaysTest/rc/spec/BarclaysWidgetSpec'], function (promiseUtils) {
  'use strict';

  beforeEach(function () {
    promiseUtils.addCustomMatchers(this);
  });
});
